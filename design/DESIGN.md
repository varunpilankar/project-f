# Design system — PROJECT F (2/2)

Generated by the designer agent. This is the approved look of the product;
implement against it rather than inventing styling per ticket.

- Platform: **web**
- Stylesheet: `design/design-system.css` (36137 chars, 171 classes)
- Entry screen: `baby-registry-wishlist`

## How to use this

1. Serve `design/design-system.css` as a real stylesheet and link it from your
   base layout. Do not paste its rules into templates.
2. Reuse the class names below. They already match the mockups.
3. The screens are **mockups**: their content is placeholder. Copy the
   structure and classes, never the data — render real records through the
   framework's templating.

## Tokens

| Token | Value |
| --- | --- |
| `--color-primary-50` | `#FFF5F0` |
| `--color-primary-100` | `#FFE6DB` |
| `--color-primary-200` | `#FFCCB8` |
| `--color-primary-500` | `#F48C68` |
| `--color-primary-600` | `#DE724E` |
| `--color-primary-700` | `#BD5332` |
| `--color-sage-50` | `#F3F8F4` |
| `--color-sage-100` | `#E4EFE6` |
| `--color-sage-500` | `#7CA982` |
| `--color-sage-600` | `#628E68` |
| `--color-butter-50` | `#FFFDED` |
| `--color-butter-100` | `#FEF9D4` |
| `--color-butter-400` | `#FAD663` |
| `--color-butter-500` | `#F4C430` |
| `--color-lavender-50` | `#F8F5FF` |
| `--color-lavender-100` | `#ECE4FD` |
| `--color-lavender-500` | `#9B7EDA` |
| `--color-bg-cream` | `#FDFBF7` |
| `--color-bg-card` | `#FFFFFF` |
| `--color-bg-subtle` | `#F7F5F0` |
| `--color-text-main` | `#2C3539` |
| `--color-text-muted` | `#677482` |
| `--color-text-light` | `#94A3B8` |
| `--color-border` | `#EAE6DF` |
| `--color-border-focus` | `#F48C68` |
| `--color-success` | `#2E7D32` |
| `--color-success-bg` | `#E8F5E9` |
| `--color-badge-age` | `#4338CA` |
| `--color-badge-age-bg` | `#EEF2FF` |
| `--font-family` | `'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` |
| `--font-heading` | `'Quicksand', 'Plus Jakarta Sans', system-ui, sans-serif` |
| `--shadow-xs` | `0 1px 2px rgba(44, 53, 57, 0.04)` |
| `--shadow-sm` | `0 2px 6px rgba(44, 53, 57, 0.06)` |
| `--shadow-md` | `0 8px 20px -4px rgba(244, 140, 104, 0.12), 0 4px 12px rgba(44, 53, 57, 0.05)` |
| `--shadow-lg` | `0 16px 32px -8px rgba(44, 53, 57, 0.12)` |
| `--shadow-card-hover` | `0 14px 28px -6px rgba(244, 140, 104, 0.18), 0 6px 14px rgba(44, 53, 57, 0.06)` |
| `--radius-xs` | `6px` |
| `--radius-sm` | `10px` |
| `--radius-md` | `16px` |
| `--radius-lg` | `24px` |
| `--radius-pill` | `9999px` |
| `--transition-fast` | `0.18s ease-in-out` |
| `--transition-normal` | `0.28s cubic-bezier(0.16, 1, 0.3, 1)` |

## Shared elements

Present on every screen — build these once, as a base layout / partial.

- **Announcement Banner** (`top`) — `design/common/Announcement-Banner.html`
- **Main Header** (`top`) — `design/common/Main-Header.html`
- **Main Footer** (`bottom`) — `design/common/Main-Footer.html`

## Screens

| Screen | Type | Mockup |
| --- | --- | --- |
| Baby Registry & Wishlist | screen | `design/screens/baby-registry-wishlist.html` |
| Cart & Checkout (Guest & Stripe/UPI) | screen | `design/screens/cart-checkout.html` |
| Order Confirmation & Tracking | screen | `design/screens/order-confirmation-tracking.html` |

## Classes

`.acc-tab-btn`, `.account-type-toggle`, `.action-buttons-group`, `.action-count`, `.add-registry-banner`, `.age-chip`, `.age-chips`, `.age-pills-nav`, `.announcement-banner`, `.announcement-container`, `.announcement-content`, `.announcement-link`, `.announcement-links`, `.badge-pill`, `.bg-butter`, `.bg-peach`, `.bg-sage`, `.brand-group`, `.brand-logo`, `.brand-name`, `.brand-tagline`, `.btn-gift-purchased`, `.btn-gift-this`, `.btn-outline-action`, `.btn-qr`, `.btn-whatsapp`, `.card-brand-pill`, `.cart-badge`, `.cart-icon-box`, `.cart-info-text`, `.cart-item-details`, `.cart-item-img`, `.cart-item-price`, `.cart-item-sub`, `.cart-item-title`, `.cart-label`, `.cart-price`, `.cart-trigger-btn`, `.category-subnav`, `.checkout-card-section`, `.checkout-cart-item`, `.checkout-cart-list`, `.checkout-grid`, `.checkout-section-title`, `.checkout-stepper-bar`, `.confetti-icon-wrap`, `.copy-link-btn`, `.cost-row`, `.courier-details-bar`, `.courier-map-card`, `.courier-name`, `.courier-van-marker`, `.deals-pill`, `.destination-pin`, `.details-address-block`, `.divider`, `.est-delivery-text`, `.footer-bottom`, `.footer-bottom-links`, `.footer-col`, `.footer-desc`, `.footer-logo`, `.footer-main`, `.footer-top-perks`, `.form-group`, `.form-input`, `.form-row-2`, `.gift-checkbox-row`, `.gift-note-textarea`, `.gift-option-card`, `.gift-price`, `.group-gift-box`, `.group-gift-title`, `.guarantee-note`, `.header-actions`, `.header-inner`, `.header-search-input`, `.icon-btn`, `.item-age-spec`, `.items-count-tag`, `.live-timeline-list`, `.logo-icon-wrap`, `.map-visual-placeholder`, `.nav-label`, `.newsletter-btn`, `.newsletter-form`, `.newsletter-input`, `.newsletter-text`, `.node-current`, `.node-done`, `.node-future`, `.order-details-card`, `.order-ref-pill`, `.order-success-hero`, `.order-summary-sidebar`, `.order-summary-title`, `.ordered-item-row`, `.ordered-item-thumb`, `.page-container`, `.pay-chip`, `.pay-icon-row`, `.pay-label-bold`, `.payment-badges`, `.payment-method-card`, `.payment-tabs-header`, `.perk-card`, `.perk-icon`, `.place-order-big-btn`, `.priority-badge`, `.progress-bar-fill`, `.progress-bar-track`, `.promo-apply-btn`, `.promo-code-box`, `.promo-input`, `.registry-avatar-large`, `.registry-badge-host`, `.registry-filter-nav`, `.registry-header-flex`, `.registry-hero-banner`, `.registry-info-left`, `.registry-item-actions`, `.registry-item-card`, `.registry-item-content`, `.registry-item-meta`, `.registry-item-thumb`, `.registry-items-grid`, `.registry-meta-item`, `.registry-meta-row`, `.registry-progress-header`, `.registry-stats-box`, `.registry-stats-counts`, `.registry-tab`, `.registry-tabs-group`, `.registry-title`, `.search-bar-wrap`, `.search-icon`, `.share-generator-card`, `.share-icon-btn`, `.share-input-box`, `.share-link-group`, `.share-social-actions`, `.site-footer`, `.site-header`, `.spam-free`, `.stat-bold`, `.status-badge-fulfilled`, `.status-badge-needed`, `.status-pill-in-transit`, `.step-num`, `.stepper-divider`, `.stepper-step`, `.stripe-container-box`, `.stripe-header-row`, `.stripe-logos`, `.subnav-inner`, `.subnav-link`, `.summary-cost-breakdown`, `.timeline-content-body`, `.timeline-desc`, `.timeline-item`, `.timeline-node`, `.timeline-time`, `.tracking-header-status`, `.tracking-layout-grid`, `.tracking-timeline-card`, `.upi-app-badge`, `.upi-apps-row`, `.upi-container-box`, `.upi-qr-code-img`, `.upi-qr-scanner-box`, `.user-action-buttons`
